/* document.addEventListener('keyup', function(e) {
    // Verifique se as teclas pressionadas correspondem ao atalho desejado
    if (e.key === 'ArrowLeft') {
        tutorial1Audio.play();
    } else {
        console.log('não funcionou');
    }
}); */
